[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."epg"]
  # 7 days of data per country can be large; give it room
  included_files = []
